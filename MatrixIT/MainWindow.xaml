<Window x:Class="MatrixIT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ПАК Матрикс: Мониторинг счетчиков воды" Height="350" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выбор объекта учета:" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox x:Name="MeterSelector" Grid.Row="1" Margin="0,0,0,10" 
                  ItemsSource="{Binding MeterList}" DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedMeter}"/>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <StackPanel>
                <TextBlock Text="Текущие показания:" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding CurrentReading, StringFormat={}{0:F2} м³}" 
                           FontSize="24" Foreground="DarkBlue" Margin="0,5"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{Binding StatusColor}"/>
                <TextBlock Text="{Binding LastUpdateTime, StringFormat='Обновлено: {0:dd.MM.yyyy HH:mm:ss}'}" 
                           FontSize="10" Foreground="Gray" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Обновить данные" Width="120" Command="{Binding RefreshCommand}" Margin="0,0,10,0"/>
            <Button Content="Отправить отчет" Width="120" Command="{Binding SendReportCommand}"/>
        </StackPanel>
    </Grid>
</Window>